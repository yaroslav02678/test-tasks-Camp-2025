name: Build Project

on:
  push:
    branches:
      - 'Task_2'  

jobs:
  build:
    runs-on: windows-latest 

    steps:
      - name: Check out repository
        uses: actions/checkout@v4  

      - name: Build Project
        run: |
          cd Task_2
          mkdir build
          cd build
          cmake ..
          cmake --build .
          
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
                Task_2/build/Debug/GameOfLife.exe
                Task_2/build/Debug/TestDemoVersion.exe
                Task_2/build/Debug/Test1.exe
                Task_2/build/Debug/Test1.txt

      - name: Run tests
        run: |
          cd Task_2/build/Debug
          ./Test1.exe